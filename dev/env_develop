export TEST_MODE=1

export POSTGRES_HOSTNAME=127.0.0.1
export POSTGRES_PORT=5439
export POSTGRES_USER=user
export POSTGRES_PASSWORD=password
export POSTGRES_DB=test_database
export POSTGRES_DEBUG=True

export POSTGRES_URI=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOSTNAME}:${POSTGRES_PORT}/${POSTGRES_DB}
